spring:
  application:
    name: mpay-payment
  datasource:
    url: jdbc:mysql://localhost:3307/mpay?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: rootpass
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
  redis:
    host: localhost
    port: 6380

server:
  port: 8100
